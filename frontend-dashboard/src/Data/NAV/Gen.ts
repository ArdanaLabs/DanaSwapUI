import { Arbitrary, float } from "fast-check"
//import { NAV, isoNAV } from "Data/NAV"

//export function genNAV<A>(Arb: Arbitrary<A>): Arbitrary<NAV<A>> {
//  return Arb.generate().map(isoNAV.wrap)
//}
